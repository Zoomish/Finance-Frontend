import { FC, useState } from 'react'

const Auth: FC = () => {
    const [isLogin, setIsLogin] = useState<boolean>(true)
    return (
        <div>
            <h1>Login</h1>
            {/* Implement login form */}
        </div>
    )
}

export default Auth
