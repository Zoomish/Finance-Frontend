import { FC } from 'react'

const Header: FC = () => {
    return (
        <header className="flex items-center justify-between bg-slate-800 px-4 py-2 shadow-sm backdrop-blur-sm"></header>
    )
}

export default Header
